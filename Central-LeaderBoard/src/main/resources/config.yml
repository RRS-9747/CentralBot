Version: '${project.version}'

Config:
  Command: "leaderboard"

Embed:
    Title: "🏆 ``{BOARD}`` Leaderboard ({TIME}) 🏆"
    Footer:
      Text: "{AUTHOR}"

Leaderboard:
  Format: "{NAME} -> {VALUE} {BOARD}"
  Top: 10